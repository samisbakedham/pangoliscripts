import { OperationType, SafeTransaction } from '@gnosis.pm/safe-core-sdk-types';
import EthAdapter from 'ethereumLibs/EthAdapter';
import GnosisSafeContract from '../../contracts/GnosisSafe/GnosisSafeContract';
export declare function estimateTxGas(safeContract: GnosisSafeContract, ethAdapter: EthAdapter, to: string, valueInWei: string, data: string, operation: OperationType): Promise<number>;
export declare function estimateGasForTransactionExecution(safeContract: GnosisSafeContract, from: string, tx: SafeTransaction): Promise<number>;
